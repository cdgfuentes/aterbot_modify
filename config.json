{
	"client": {
	  "host": "${MINECRAFT_CLIENT_HOST}",
	  "port": "${MINECRAFT_CLIENT_PORT'}",
	  "username": "${MINECRAFT_USERNAME}"
	},
	"logLevel": "${LOG_LEVEL || 'error,log,debug'}",
	"action": {
	  "commands": "${ACTION_COMMANDS || 'forward,back,left,right,jump'}",
	  "holdDuration": 348000,
	  "retryDelay": 15000
	}
  }
  